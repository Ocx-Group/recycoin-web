apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ecosystem

resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - frontend-ingress.yaml

images:
  - name: registry.digitalocean.com/ocx-registry/recycoin-web
    newTag: latest
